import React from 'react'; import PropTypes from 'prop-types'; import './FlagStyle.scss';export default function Flag(props) {    let activeMode = ""    switch (props.active) {        case true: activeMode = "active"; break;        case false: activeMode = ""; break;    }    return (        <div className={`flag ${activeMode} ${props.className}`}>            <a href={props.href}><img src={props.src} alt={props.alt}/></a>        </div>     ); }Flag.propTypes = {    href: PropTypes.string,    src: PropTypes.string,    alt: PropTypes.string,    active: PropTypes.bool,}Flag.defaultProps = {    href: "",    src: "",    alt: "",    className: "",    active: "",}