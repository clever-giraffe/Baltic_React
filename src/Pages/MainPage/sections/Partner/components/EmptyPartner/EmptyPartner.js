import React from 'react'; import PropTypes from 'prop-types'; import './EmptyPartnerStyle.scss';export default function EmptyPartner(props) {    let temp = "big" ;   /* switch (props.type) {        case "small":  temp = "small"; break;        case "big":  temp = "big"; break;    }*/    if (props.type==="small"){        temp = "small"    }    return (        <div >            <div className={`EmptyPartner ${temp} ${props.className}`}>            </div>        </div>    ); }EmptyPartner.propTypes = {    type: PropTypes.string,    className: PropTypes.string,}EmptyPartner.defaultProps = {    type: "big",    className: "",}